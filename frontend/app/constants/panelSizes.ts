export const PANEL_SIZES = {
  /** the app-wide content width: preferences pages, Data Management, Audits,
   *  the Issues list. Settings surfaces fill it too — a settings row keeps its
   *  control directly under its label rather than pinned to the right edge, so
   *  there is nothing to maroon at this width (Gabriel 07-30). */
  maxWidth: '1360px',
};
